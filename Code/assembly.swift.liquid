//
//  {{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import Swinject

class {{ module_info.name }}Assembly: Assembly {
	func assemble(container: Container) {
		// register navigator
		container.register({{ module_info.name }}Navigator.self) { (_, view: {{ module_info.name }}ViewInput) in
			return {{ module_info.name }}Navigator(sourceViewController: view as? {{ module_info.name }}ViewController)
		}
		
		// register presenter
		container.register({{ module_info.name }}ViewOutput.self) { (_, view: {{ module_info.name }}ViewInput) in
			return {{ module_info.name }}Presenter(view: view)
		}
		
		// view
		container.storyboardInitCompleted({{ module_info.name }}ViewController.self) { (resolver, vc: {{ module_info.name }}ViewInput) in
			if let presenter = resolver.resolve({{ module_info.name }}ViewOutput.self, argument: vc) as? {{ module_info.name }}Presenter {
				presenter.navigator = resolver.resolve({{ module_info.name }}Navigator.self, argument: vc)
				vc.set(presenter: presenter)
			}
			vc.notifying = resolver.resolve(NotificationService.self)
		}
	}
}